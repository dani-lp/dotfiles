# ls aliases
alias ls='ls -F --sort=extension --color=auto'
alias ll='ls -l'
alias lsl='ls -alF'
alias la='ls -A'
alias lsa='ls -al'
alias l='ls -CF'

# enable color support of ls and also add handy aliases
# if [ -x /usr/bin/dircolors ]; then
#     test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
#     alias dir='dir --color=auto'
#     alias vdir='vdir --color=auto'
# 
#     alias grep='grep --color=auto'
#     alias fgrep='fgrep --color=auto'
#     alias egrep='egrep --color=auto'
# fi

# To avoid mistakes
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Git commands (to be revised)
alias gpo='git push origin master'
alias gp='git pull'
alias gs='git status'
alias gd='git diff'
alias gr='git reflog'
alias glf='git ls-files'
alias gc='git clone'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
# alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

# QoL stuff
alias c='clear'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias sl='sl -e'
alias fucking='sudo'
alias colortest='npx colortest'
alias clnf='clear && neofetch'
alias y='echo Hey $USERNAME, you can stop spamming Y'
alias diskspace='du -S | sort -n -r |more'
alias topdf='libreoffice --headless --invisible --convert-to pdf'
alias toilet='toilet -f mono12 -F metal'
alias bat='batcat'
alias cat='batcat'

# I really need to understand how PATH works
alias spicetify='/home/dani/spicetify-cli/spicetify'
alias besu='~/dev/hyperledger/besu-21.7.3/bin/besu'

# Quickly load bashrc
alias gogo='source ~/.bashrc'
